# Codex 项目约定

## 接手顺序

每次在新任务或新电脑上接手本项目，先完成以下只读检查：

1. 阅读本文件、`README.md` 与 `docs/PROJECT_STATUS.md`。
2. 运行 `git status --short`，确认是否存在上一台电脑遗留的未提交改动。
3. 查看最近提交：`git log --oneline -5`。
4. 若工作树干净且用户明确要求同步，再从当前远端拉取；不要擅自覆盖或清理已有改动。

聊天记录不是项目状态的唯一来源。发生冲突时，以仓库文件、最近提交和用户当前指令为准。

## 项目目标

为伯特兰·罗素《西方哲学史》制作中文交互学习笔记。用户通过历史进入哲学，首要目标是用最低记忆成本建立高密度、可下钻、可复用的知识关系。

首要骨架始终是历史与历史阶段：

`历史阶段 → 历史事件与时代问题 → 哲学流派 → 哲学家 → 概念与论证 → 原书章节`

“历史概览”是默认主视图；原有 9 个基于罗素目录的重构框架作为数据层嵌入历史概览，不再维护独立“原书结构”页面；“原书索引”保持原书卷、部、章顺序；“哲学家”负责跨章节重组人物知识。

## 内容与研究规则

- 以罗素原书为主要叙述来源，但不能把罗素的个人判断写成学界共识。
- 网络补充优先使用 SEP、IEP、权威出版社、博物馆、大学或原始文献；保存可点击来源。
- 明确区分：原书叙述、一般史实、现代研究校正、本站学习重构。
- 明确区分：直接影响、共同传统、同题比较、批评关系、后世重构。
- 不把不可靠古代轶事写成确定履历；使用“较可靠／传统记载／推定”等证据等级。
- 哲学观点优先组织成“对象—问题—逻辑起点—推导—概念—结论”，避免长篇复述。
- 人名、地名、哲学名词保留中英双语；解释性正文以中文为主。
- 人物图像必须保留来源、许可与“非写实肖像”提示；不要用生成图替代已有历史图像。

## 视觉与交互规则

- 用户重视信息密度：小号字必须可读，大标题克制，减少无意义留白和过度滚动。
- 新功能延续现有纸张色、墨色、暗红和深绿系统，不另起一套视觉语言。
- 关系较多时使用双列表、关系链、比较表或卡片；不把所有内容塞进连续正文。
- 人名、地名和术语应保持行内排版，不得人为插入换行。
- 从原书章节下钻后必须能回到此前阅读位置。
- 新交互需兼顾键盘、触摸屏和窄屏；弹窗至少支持关闭按钮、遮罩和 Escape。
- 除非用户明确要求，不进行浏览器截图或视觉检查；必须至少通过构建。

## 技术边界

- 保留 vinext、React、npm 与当前目录结构；不要替换框架或锁文件体系。
- 使用 `apply_patch` 编辑手写文件；不要覆盖用户未提交的无关改动。
- 网站内容数据优先放在对应的 `app/*-data.ts`，不要继续把大量资料直接写进 `app/page.tsx`。
- 本地偏好可使用 `localStorage`；不要为当前静态学习数据引入数据库。
- 人物与地图资产需要纳入 Git；生成目录和依赖目录必须保持忽略。
- `node_modules` 与 `dist` 放在项目外的本地运行目录 `/Users/simon/Library/Application Support/AHOWP-local-runtime`，项目内只保留本地链接；跨电脑时按 `docs/MULTI_MACHINE_WORKFLOW.md` 重建，不把它们同步到 OneDrive。
- `offline-reader/index.html` 是由 `npm run build:offline` 生成并纳入 Git 的交付文件。不要手工编辑；内容或样式变化后重新生成。
- 离线入口依赖相邻项目中的 `public/` 图片目录，不能只移动这一个 HTML 文件。
- 完成代码变更后运行 `npm run build`。若修改了可被 lint 检查的逻辑，再运行 `npm run lint` 并只修复与当前任务有关的问题。

## Git 交接约定

- 两台电脑按“交替”而非“同时”方式工作；同一时间只让一台电脑修改 `main`。
- 一个完整功能形成一个聚焦提交。提交前更新 `docs/PROJECT_STATUS.md`。
- 不使用 `git reset --hard`、强制推送或覆盖式 checkout。
- Codex 可以在用户要求的交接里创建本地提交；没有用户明确授权时，不向远端推送。
- 实验性或可能跨多轮的工作使用 `codex/<短任务名>` 分支，完成后再合并。

## 当前阅读入口

完整现状、已完成范围、已知缺口和下一步见 `docs/PROJECT_STATUS.md`；若需要复刻完整产品行为，继续阅读 `docs/SYSTEM_SPEC.md`。跨电脑操作流程见 `docs/MULTI_MACHINE_WORKFLOW.md`。
